
import Quick
import Nimble
@testable import MonorailSwift

class MonorailSwiftTests: QuickSpec {
    
    override func spec() {
        describe("Reader") {
            it("should support file and id reference") {
                
            }
            
            it("reader delegate works correctly") {
                
            }
        }
        
        describe("Writer") {
            it("can save network interaction to file and correctly load") {
                
            }
        }
    }
}
